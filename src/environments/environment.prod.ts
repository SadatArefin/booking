export const environment = {
    production: true,
    firebase: {
        apiKey: "YOUR_API_KEY", // Replace with your actual production API key during deployment
        authDomain: "YOUR_AUTH_DOMAIN", // Replace with your actual production auth domain during deployment
        projectId: "YOUR_PROJECT_ID", // Replace with your actual production project ID during deployment
        storageBucket: "YOUR_STORAGE_BUCKET", // Replace with your actual production storage bucket during deployment
        messagingSenderId: "YOUR_MESSAGING_SENDER_ID", // Replace with your actual production messaging sender ID during deployment
        appId: "YOUR_APP_ID", // Replace with your actual production app ID during deployment
        measurementId: "YOUR_MEASUREMENT_ID" // Replace with your actual production measurement ID during deployment
    }
};
